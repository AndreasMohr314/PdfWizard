package core;

import ui.UIManager;

public class Main {
    public static void main(String[] args) {
        // Initialize and launch the UI
        UIManager uiManager = new UIManager();
        uiManager.launch();
    }
}
