package ui;

import javafx.application.Application;

// Manages the application UI
public class UIManager {
    public void launch() {
        // Launches the MainUI JavaFX application
        Application.launch(MainUI.class);
    }
}

